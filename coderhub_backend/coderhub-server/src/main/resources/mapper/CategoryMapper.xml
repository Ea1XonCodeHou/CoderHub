<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.eaxon.coderhubserver.mapper.CategoryMapper">
    
    <!-- 动态更新分类 -->
    <update id="update">
        UPDATE coder_hub.category
        <set>
            <if test="categoryName != null">category_name = #{categoryName},</if>
            <if test="parentId != null">parent_id = #{parentId},</if>
            <if test="sortOrder != null">sort_order = #{sortOrder},</if>
            <if test="description != null">description = #{description},</if>
            <if test="icon != null">icon = #{icon},</if>
            <if test="status != null">status = #{status},</if>
            <if test="articleCount != null">article_count = #{articleCount},</if>
            <if test="referenceCount != null">reference_count = #{referenceCount},</if>
            <if test="viewCount != null">view_count = #{viewCount},</if>
        </set>
        WHERE id = #{id}
    </update>
    
</mapper>

